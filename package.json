{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "yarn workspace protoc-gen-nexus build",
    "lint": "yarn workspace protoc-gen-nexus lint",
    "test": "yarn workspace protoc-gen-nexus test"
  },
  "eslintConfig": {
    "extends": [
      "wantedly-typescript"
    ],
    "parserOptions": {
      "project": [
        "./tsconfig.json"
      ]
    },
    "rules": {
      "import/no-default-export": "error"
    }
  }
}
